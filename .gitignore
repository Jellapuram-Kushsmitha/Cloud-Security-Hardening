# Terraform state files — contain sensitive data
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
tfplan

# AWS credentials — never commit
.aws/
*.pem
*_key.pem
credentials

# Secrets
*.secret
secrets/
.env
.env.*

# OS files
.DS_Store
*.swp
__pycache__/
bin/
docker/trivy-report.json
